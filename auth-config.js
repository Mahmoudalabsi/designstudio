/**
 * ───────────────────────────────────────────────────────────
 *  إعدادات المصادقة - مصمم الصور
 * ───────────────────────────────────────────────────────────
 *  هذا الملف يعمل كـ "قاعدة بيانات" لكلمة المرور المشتركة.
 *  كلمة المرور لا تُخزَّن كنص واضح، بل كتجزئة SHA-256 مع ملح (salt).
 *
 *  لتغيير كلمة المرور:
 *  1. افتح صفحة set-password.html في المتصفح
 *  2. أدخل كلمة المرور الجديدة
 *  3. انسخ الكود الناتج واستبدل به قيم passwordHash و salt أدناه
 *
 *  كلمة المرور الافتراضية الحالية: admin123
 *  ⚠️ يجب تغييرها فوراً بعد الإعداد!
 * ───────────────────────────────────────────────────────────
 */
window.AUTH_CONFIG = {
  // تجزئة SHA-256 لـ (salt + password)
  passwordHash: '0b0d3504a022f18fd38a788135a4efe15c4469826126759647fdaf5dea9f4193',

  // الملح العشوائي المضاف قبل كلمة المرور
  salt: 'a152d876117d4a91fcda87f2a1899edc',

  // مدة الجلسة بالمللي ثانية (24 ساعة)
  sessionDuration: 24 * 60 * 60 * 1000,

  // صفحة تسجيل الدخول
  loginPage: 'login.html',

  // الصفحات التي لا تحتاج مصادقة (مثل صفحة تسجيل الدخول نفسها)
  publicPages: ['login.html', 'set-password.html'],

  // مفتاح تخزين الجلسة في localStorage
  storageKey: 'musammer_auth_session'
};
